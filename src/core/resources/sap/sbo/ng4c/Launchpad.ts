/// <reference path="baseelement.ts" />
module sap.sbo.ng4c {
    import BaseElement = sap.sbo.ng4c.BaseElement;
    export class Launchpad extends BaseElement {
        public constructor($scope: Object, $element: JQuery) {
            super($scope, $element);
        }
    }
}